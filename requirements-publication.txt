# Runtime requirements for the publication finalization and audit gate.
# Install in a clean environment before running:
#   scripts\run_publication_gate.cmd

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
SciencePlots>=2.2
adjustText>=1.4
Pillow>=10.0
rasterio>=1.3
scipy>=1.10
