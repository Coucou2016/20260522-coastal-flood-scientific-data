# Finalization blocker

The manuscript, figure-generation scripts, publication-readiness audit, source-data dictionary, reviewer-risk register and finalization workflow have been prepared. The remaining required step is to execute the finalization workflow and inspect the generated outputs.

## Blocking condition

The local command-execution layer repeatedly rejects process creation with:

```text
Failed to create unified exec process: setup refresh failed with status exit code: 1
```

This prevents the final workflow from being run inside the current agent session.

## Required command after execution is restored

Run from the project root:

```powershell
python scripts\finalize_cee_publication_package.py
```

If PowerShell remains unavailable, run:

```cmd
scripts\run_finalization.cmd
```

## Expected outputs

The finalization workflow intentionally removes stale `publication_package/`, `publication_package.zip`, and previous failure-status files before verification begins. If a run fails early, the absence of an old package is expected and prevents stale results from being mistaken for current outputs.

- `manuscript/publication_readiness_report.md`
- refreshed `manuscript/process_terrain_coastal_flood_CEE_manuscript_review.html`
- refreshed `figures/main/Fig1_process_terrain_design.png` and `.pdf`
- refreshed `figures/main/Fig2_water_level_divergence.png` and `.pdf`
- refreshed `figures/main/Fig3_meteorological_coherence.png` and `.pdf`
- refreshed `figures/main/Fig4_deltadtm_terrain_sensitivity.png` and `.pdf`
- refreshed `figures/main/Fig5_process_terrain_typology.png` and `.pdf`
- refreshed source CSV files under `data/figure_source/`
- `publication_package/` containing the compact manuscript package
- `publication_package.zip` containing a portable copy of the compact manuscript package

## Completion criteria

The work should not be treated as final until:

1. `scripts/finalize_cee_publication_package.py` exits with code 0.
2. `scripts/audit_publication_readiness.py` reports `PUBLICATION READINESS AUDIT: PASS`.
3. The generated HTML embeds the current main figure PNG files as Base64 images with no external dependencies.
4. Figure 4 and Figure 5 reflect the boundary-seeded ocean-connected terrain algorithm.
5. Table 4 reports `Median coastal-lowland elevation (m)` rather than capped full-window median elevation.
6. The final manuscript contains no unresolved submission placeholders.
7. If finalization fails, inspect `logs/cee_publication_finalize_failed.md` and `publication_package_failed_status.json`, then resolve every listed failed check before treating the manuscript package as current. If the failed-status JSON reports `safe_to_use_existing_package: false`, do not use any existing `publication_package/` or `publication_package.zip` as current output.
8. `publication_package.zip` exists and contains the final HTML, main figures, figure-source CSV files, audit report and package manifest.

## Current status

Blocked by execution environment, not by known manuscript logic. The next meaningful step is to restore command execution and run the finalization workflow.
## Current execution blocker

The manuscript, figure, audit, and package scripts are prepared for the final publication run, but this Codex session cannot currently start local command processes. The repeated failure is:

```text
Failed to create unified exec process: setup refresh failed with status exit code: 1
```

Because of that environment-level failure, do not treat any existing `publication_package/` or `publication_package.zip` as freshly regenerated by this session. The first command to run after the execution environment is restored is:

```cmd
scripts\run_finalization.cmd
```

A successful finalization run must regenerate the refined figures, rebuild the manuscript review HTML, run the publication readiness audit, create `publication_package/`, create `publication_package.zip`, write `completion_status.json` with finalized status, write root-level `final_archive_manifest.json`, and pass `scripts\validate_publication_package.cmd`.
## Recovery command

After the command-execution environment is restored, the preferred recovery command is:

```cmd
scripts\run_publication_gate.cmd
```

This command produces `logs\publication_gate.txt`, `logs\publication_gate.json`, `logs\manuscript_figure_inventory.md`, `logs\reference_metadata_audit.txt`, `logs\journal_submission_metadata_audit.txt`, `logs\repository_deposit_readiness_audit.txt`, `logs\publication_readiness_summary.md`, `logs\final_evidence_index.md`, `logs\archive_handoff_audit.txt`, and `publication_gate_evidence\`. Those reports and copied evidence files are the clearest local evidence that manuscript figure inventory, manuscript text audit, reference metadata audit, journal submission metadata audit, figure/source data audit, cross-artifact consistency audit, repository deposit readiness, package generation, archive validation, final handoff boundary checks, and evidence indexing all passed in one sequence.
